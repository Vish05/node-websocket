
interface UpdateDisplayNameProps {
  name: string
  gameId?: string
  playerSessionId?: string
}

export async function updateDisplayName({ name, gameId, playerSessionId }: UpdateDisplayNameProps) {
  
  return "vishal"
}
